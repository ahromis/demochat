apiVersion: apps/v1
kind: Deployment
metadata:
  name: demochat
spec:
  selector:
    matchLabels:
      app: demochat
      tier: frontend
  replicas: 1
  template:
    metadata:
      labels:
        app: demochat
        tier: frontend
    spec:
      containers:
      - name: demochat
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: "{{ .Values.image.pullPolicy }}"
        ports:
        - containerPort: 5000
