apiVersion: v1
kind: Service
metadata:
  name: demochat
  labels:
    app: demochat
    tier: frontend
spec:
  type: {{ .Values.service.type }}
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
  selector:
    app: demochat
    tier: frontend
